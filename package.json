{
  "name": "redlink",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip:processOrders": "powershell Compress-Archive -Path src\\handlers\\processOrders.js -DestinationPath build\\processOrders.zip -Force",
    "zip:health": "powershell Compress-Archive -Path src\\handlers\\health.js -DestinationPath build\\health.zip -Force",
    "zip:submitOrder": "powershell Compress-Archive -Path src\\handlers\\submitOrder.js -DestinationPath build\\submitOrder.zip -Force",
    "zip:getOrder": "powershell Compress-Archive -Path src\\handlers\\getOrder.js -DestinationPath build\\getOrder.zip -Force",
    "zip:all": "npm run zip:processOrders && npm run zip:health && npm run zip:submitOrder && npm run zip:getOrder",
    "clean": "rimraf build/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.10"
  }
}
